{mode, max}.

{duration, 1}.
{report_interval,1}.

{concurrent, 3}.

{driver, basho_bench_driver_redis}.

{key_generator, {uniform_int, 1000000}}.

{value_generator, {fixed_bin, 1000}}.

{redis_ips, ["45.63.89.14"]}.
{redis_port, 31482}.


%%% {operations, [{get, 1}]}.
{operations, [{put, 1}]}.
