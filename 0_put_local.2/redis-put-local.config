{mode, max}.

{duration, 1}.
{report_interval,1}.

{concurrent, 3}.

{driver, basho_bench_driver_redis}.

{key_generator, {uniform_int, 10000}}.

{value_generator, {fixed_bin, 1000}}.

{redis_ips, ["127.0.0.1"]}.
{redis_port, 6379}.


%%% {operations, [{get, 1}]}.
{operations, [{put, 1}]}.
